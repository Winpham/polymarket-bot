# PRE-REGISTRATION — "Beat the Best Tracked Trader" autonomous run

**Frozen UTC:** 2026-07-05T18:30:36Z
**Branch:** run/beat-best-trader
**Rule:** all benchmark/gate reads that claim OUT-OF-SAMPLE status filter `first_detected_at ≥` this
timestamp. Hypotheses below are frozen BEFORE any measurement this run. No threshold is tuned to
manufacture a pass. PAPER-ONLY; nothing is promoted to real money; no Rust threshold is mutated
(D29 Phase-1 STOP stands — `refresh_router_followset` frozen at 0.30/0.25/0.50).

## Measurement environment
- Read-only against prod container `polymarket-bot-postgres-1` (no `pg-report` snapshot is cheaply
  available; the instruments are read-only by construction — ZERO write statements issued).
- Capture flags live at run start: `DENSE_CAPTURE=true`, `CONSENSUS_TRUST_ARMS=true` already ON in
  the running `polymarket-bot-copy-trading-bot-1` container. `SLICE_POOLED` is OFF (default) and is
  NOT flipped this run — enabling it requires editing the main-repo compose + rebuilding the live
  paper bot (a main-checkout change the isolation rule warns against), and the measurement core does
  not depend on it. Recorded as deferred. Dense-capture trajectory is confirmed accruing
  (signal_price_trajectory: 149 signals, 2762 rows, 2026-07-03 → 2026-07-05).

## Data state at freeze (context for INDETERMINATE-BY-POWER priors)
- Paper ledger (`honest_paper_ledger`): 2026-07-01 → 2026-07-05 (~5 correlated days). Only `favorite`
  (avg pnl +5.60 / 124 bets) and `elite_fresh_fav` (−1.77 / 55) are non-negative; broad arms ≈ −15%.
- `trader_fills`: 2.47M fills, 491 wallets, 1.69M resolved, max ts 2026-07-05. Sports present:
  soccer (dominant, 405 wallets), crypto, tennis, other, mlb, lol, cs2, nba, politics, ufc, dota,
  nhl, nfl. Cross-context (sport-regime) variance EXISTS → H3 LOO is decidable; cross-time accrual
  (independent day-clusters) is still ~a handful → temporal persistence remains power-starved.

## Pre-registered hypotheses (frozen)
- **H1** (router beats fleet): per-context argmax-wallet router, priced at OUR realizable entry, beats
  the fleet-average consensus arms in OUT-OF-SAMPLE event-clustered realizable surplus.
  Prior: INDETERMINATE-BY-POWER.
- **H2** (router matches/beats best trader): router OUT realizable surplus ≥ B_LB (best single wallet
  re-priced at our entry), overlapping-or-better CI. Prior: INDETERMINATE-BY-POWER.
- **H3** (routing beats averaging — THE NOW EXPERIMENT): in a within-trader Leave-One-SPORT-REGIME-Out
  split, argmax-routing yields higher held-out realizable surplus than fleet-averaging. Uses
  cross-context variance (available now). CAN return a SIGNED answer. Prior: OPEN / decidable.
- **H4** (skill-concentration on FLB band): concentrating weight (TrustWeighted/CellPooled) on the
  favorite band 0.65–0.98 beats blind-by-band AND beats the best single favorite-tailer's B_LB.
  Prior: INDETERMINATE-BY-POWER.
- **H5** (fade beats tail on overhype): FADING one-sided band5 heavy-favorite consensus (buy NO at our
  price) beats band-blind baseline AND transfers to ≥1 sport-regime OUTSIDE the discovery cell.
  Prior: INDETERMINATE-BY-POWER.
- **H6** (λ̂ is real): with DENSE_CAPTURE on, measured λ̂ CI-lower crosses 0.25. Prior: expect still
  INDETERMINATE on ~4 days — report CI width + dense-capture coverage % as the finding.
- **H7** (filter by profit-source, not order-type — MM-screen refinement): the eligibility screen that
  maximizes the followed cohort's forward copy-return EXCLUDES pure market-makers (high two_sided_rate,
  ≈0 directional net_maker) while KEEPING directional predictors who post maker orders to dodge fees.
  Concretely: round_trip-relaxed screen (τ_rt 0.30→0.50, keep two_sided + sell_buy) yields ≥ the frozen
  screen's cohort forward copy-return; AND a profit-source test (regime_net_edge net_maker ≈ 0 with
  two_sided high ⇒ pure MM) agrees with the two_sided axis on the arber class. Null: matched-subset
  removal (equal-N, matched on volume/n_positions, ≥2000×, as-of/leak-free). Cohort-return DIRECTION
  is decidable NOW; persistence-lift is not. Forward-confirm before any Rust change.

Pre-registered prior for H1,H2,H4,H5,H6 = INDETERMINATE-BY-POWER. H3 = signed answer expected.
H7 cohort-return direction = decidable now; persistence = INDETERMINATE-BY-POWER.

## Eligibility screen (frozen, used everywhere: benchmark B_LB + router membership)
Keep wallet iff NOT a pure market-maker, decided by BOTH:
1. **Relaxed microstructure** (prereg default): `is_mm` with τ_rt RELAXED 0.30→0.50, τ_2s=0.25,
   τ_sb=0.50. (round_trip is a dominated FP-axis; two_sided carries the validated arb signal.)
   Confirm via `mm_screen_effect.py` that relaxed cohort forward copy-return ≥ frozen BEFORE adopting.
2. **Profit-source test** (decisive on disagreements): pure MM iff two_sided/price-sum-arb profit
   dominates AND one-sided directional re-priced net_maker (`regime_net_edge.py`) ≈ 0. KEEP any wallet
   with positive re-priced directional edge, maker orders and all. On (1)/(2) disagreement, (2) wins,
   reason logged (the 40-restore / 92-exclude reconciliation pool).
Also cross-exclude the repo's other detector (`followed_traders.trader_type='bot'`) — membership
excludes the UNION.

## Gate (every candidate clears ALL — no exceptions)
promotion_verdict (distinct_events ≥ 30; Bonferroni α/n; day-deflated SE; LB > margin 0.03;
selection_null p ≤ 0.01 checked first) · selection_null.py --calibrate PASS · ≥2 DISJOINT sport-regimes
positive · pilot_verdict (honest LB > 0.02; ≥50 events; ≥5 positive day-regimes; ≥70% regimes positive;
median-sharp liquidity ≥ $2000) · beats_best_trader (S.honest_roi_LB > B_LB + 0.03 AND S.sharpe >
B_sharpe) · λ̂ CI-lower > 0.25. Fail ANY ⇒ HOLD / INDETERMINATE-BY-POWER, never promoted.

## Self-veto (promote NOTHING if ANY holds)
distinct_days < 5 · λ̂ CI-lower < 0.25 · < 2 disjoint sport-regimes positive · selection_null
--calibrate FAIL · N_elig too thin for a meaningful B_LB. Mark INDETERMINATE-BY-POWER with the binding
reason. NEVER tune a threshold to force a pass (the refuted market_resid false-promote class).
